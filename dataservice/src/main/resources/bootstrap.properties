# we need these properties to find our settings in the config server
spring.application.name=dataservice
spring.profiles.active=local

# we define the server web port initially in the bootstrap so the service can start without the config server running
server.port=9000
